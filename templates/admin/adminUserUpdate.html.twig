{% extends 'baseAdmin.html.twig' %}

{% block title %}Modification du rôle{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-6">
                {{form_start(adminUser)}}
                    {{form_widget(adminUser)}}
                    <button class="btn btn-success">Envoyer</button>
                {{form_end(adminUser)}}
            
            </div>
        </div>
    </div>


{% endblock %}